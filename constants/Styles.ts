import {
    StyleSheet,
  } from "react-native";
const globalStyles = StyleSheet.create({
    input: {
        minWidth: "80%",
        borderWidth: 1,
        borderRadius: 15,
        padding: 10,
        marginVertical: 10,
      },
      button: {
        minWidth: "30%",
        alignItems: "center",
        justifyContent: "center",
        borderRadius: 20,
        padding: 10,
      },
})

export default globalStyles